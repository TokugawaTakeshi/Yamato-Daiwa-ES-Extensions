# `getLastElementOfNonEmptyArray`: Get last element of non-empty array

The alternative for `targetArray[targetArray.length - 1]`, herewith if array is empty, **UnexpectedEventError** error
will be thrown.
