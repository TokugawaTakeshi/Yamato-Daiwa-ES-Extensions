import type TimePoint from "./TimePoint";


const TimePointLocalization__English: TimePoint.Localization = {
  errors: {
    invalidRawDateTime: "If the parameter of TimePoint's constructor is not the Date instance, it must the valid " +
        "ISO 8601 string or amount of milliseconds elapsed since the UNIX epoch."
  }
};


export default TimePointLocalization__English;
