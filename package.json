{
  "private": true,
  "scripts": {
    "Install all dependendies": "lerna bootstrap",
    "Publish": "lerna publish --no-git-tag-version --no-private"
  },
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
